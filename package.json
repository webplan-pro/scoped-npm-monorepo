{
  "private": true,
  "devDependencies": {
    "@types/jest": "^27.4.0",
    "@types/node": "^17.0.15",
    "cross-var-no-babel": "^1.2.0",
    "jest": "^27.5.0",
    "ts-jest": "^27.1.3",
    "typescript": "^4.5.5"
  },
  "scripts": {
    "test": "jest",
    "add:package": "cross-var npm init --scope=$npm_config_scope -y -w ./packages/$npm_config_package_name"
  },
  "workspaces": [
    "./packages/*"
  ]
}
